{
  "name": "cango-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Cango — family calendar assistant (can we go?)",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
